import MaterialUiForm from "./MaterialUiForm.tsx";
import MaterialUiFormRaw from "./MaterialUiForm.tsx?raw";
import CodeSamplesLayout from '../CodeSamplesLayout.tsx'

# Form based on Material UI components

Example of using React Stateless Form with Material UI library.
Material UI form controls have `helperText` property to display text under control.
To pass error text to `helperText` custom `useConvert` hook is used with the `<Field />` component.

<CodeSamplesLayout
  codeFiles={[
    { name: 'MaterialUiForm.tsx', code: MaterialUiFormRaw },
  ]}>
  <MaterialUiForm />
</CodeSamplesLayout>