import FormWithArray from "./FormWithArray";
import FormWithArrayRaw from "./FormWithArray.tsx?raw";
import CodeSamplesLayout from "../CodeSamplesLayout.tsx";

# Form with array

To work with arrays there is a hook `useFormArray` which will return an object with methods to manage an array stored inside form.

<CodeSamplesLayout
  codeFiles={[{ name: "FormWithArray.tsx", code: FormWithArrayRaw }]}
>
  <FormWithArray />
</CodeSamplesLayout>
